# Zależności i build – NIE KOPIOWAĆ (npm install i build odtworzą)
node_modules/
dist/

# Logi i pliki tymczasowe
*.log
imprezja-electron.log
LOG-LOCATION.txt
FATAL-ERROR.txt
.DS_Store
._*
Thumbs.db

# Certyfikaty i env
certs/
.env
.env.*

# Klucz prywatny licencji (TYLKO do generowania kluczy – nie w repozytorium!)
license-private.pem

# Cursor IDE – konfiguracja lokalna, nie do repo
.cursor/
.cursorignore

# Prywatna zawartość Imprezatora – TYLKO to jest prywatne i nie trafia do builda
public/imprezator-configs/*.json

# Lokalny backup VirtualDJ do testów (np. symlink do ~/Documents/backup.zip)
tools/vdj-database-editor/test-backup-vdj.zip

# NJR Konwerter — osobny produkt; w Quiz tylko szablon (bez commitowania kodu z editor/)
njr-konwerter-1.0/editor/*
!njr-konwerter-1.0/editor/README.md
